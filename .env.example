# Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Application Settings
APP_NAME="JSON-to-SQL Analyzer for Snowflake"
APP_VERSION="1.0.0"
DEBUG=false

# Streamlit Settings
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_CONFIG_FILE=/app/config/streamlit/config.toml

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE_PATH=logs/app.log

# Performance Settings
MAX_UPLOAD_SIZE_MB=200
JSON_ANALYSIS_MAX_DEPTH=20
CACHE_TTL_SECONDS=3600

# Security Settings (if needed for future features)
# SECRET_KEY=your-secret-key-here
# API_KEY=your-api-key-here

# Database Settings (if you add database support)
# DATABASE_URL=your-database-url
# DATABASE_NAME=your-database-name